1. Logic that common to different applications
    - opening files
    - reading files
    - parsing json

    -> standalone library

2. Logic that is specific to one application, but common among front and backend
    - parsing a car object

    -> .cljc